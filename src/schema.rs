table! {
    people (id) {
        id -> Int4,
        first_name -> Varchar,
        last_name -> Varchar,
        age -> Nullable<Int4>,
        profession -> Nullable<Varchar>,
        salary -> Nullable<Int4>,
    }
}
